# =============================================================================
# Wall & Shadow — container env-file ({{ env_name }})
# =============================================================================
# Managed by Ansible. Do not edit directly on the VPS.
# Source: ansible/templates/wallandshadow.env.j2
#
# Consumed by docker run --env-file. Keep the format strict KEY=VALUE with
# no quoting, no spaces around `=`, and no shell expansion.
# =============================================================================

NODE_ENV=production
AUTH_MODE=oidc
PORT={{ app_port }}
DATABASE_URL=postgresql://{{ pg_user }}:{{ pg_password }}@localhost:5432/{{ db_name }}
JWT_SECRET={{ jwt_secret }}
S3_ENDPOINT={{ s3_endpoint }}
S3_BUCKET={{ s3_bucket }}
S3_REGION=eu-central
S3_ACCESS_KEY={{ s3_access_key }}
S3_SECRET_KEY={{ s3_secret_key }}
OIDC_ISSUER={{ oidc_issuer }}
OIDC_CLIENT_ID={{ oidc_client_id }}
