# Kamal destination: test
# Usage: kamal deploy -d test
#
# Caddy routes test.wallandshadow.com → localhost:3002

env:
  clear:
    PORT: "3002"

builder:
  args:
    VITE_DEPLOY_ENV: test
    VITE_OIDC_ISSUER: <%= ENV['OIDC_ISSUER'] %>
    VITE_OIDC_CLIENT_ID: <%= ENV['OIDC_CLIENT_ID'] %>
